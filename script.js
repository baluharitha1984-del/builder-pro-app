let options = ['Yes', 'No', 'Maybe', 'Try Again', 'Ask Later']; const wheel = document.getElementById('wheel'); const spinBtn = document.getElementById('spinBtn'); const optionsList = document.getElementById('optionsList'); const resultDisplay = document.getElementById('result'); let currentRotation = 0; function renderWheel() { wheel.innerHTML = ''; const slice = 360 / options.length; options.forEach((opt, i) => { const segment = document.createElement('div'); segment.className = 'wheel-segment'; segment.style.transform = `rotate(${i * slice}deg) skewY(${-(90 - slice)}deg)`; segment.style.backgroundColor = `hsl(${(i * 360) / options.length}, 70%, 50%)`; segment.innerHTML = `<span style="transform: skewY(${(90 - slice)}deg) rotate(${slice/2}deg); font-weight: bold;">${opt}</span>`; wheel.appendChild(segment); }); optionsList.innerHTML = options.map((opt, i) => `<li class="flex justify-between items-center p-2 bg-slate-700 rounded">${opt} <button onclick="removeOption(${i})" class="text-red-400">×</button></li>`).join(''); } window.removeOption = (i) => { options.splice(i, 1); renderWheel(); }; document.getElementById('addBtn').addEventListener('click', () => { const val = prompt('New option:'); if (val) { options.push(val); renderWheel(); } }); spinBtn.addEventListener('click', () => { const slice = 360 / options.length; const randomRot = Math.floor(Math.random() * 360) + 1440; currentRotation += randomRot; wheel.style.transform = `rotate(${currentRotation}deg)`; const normalizedRot = currentRotation % 360; const winningIndex = Math.floor((360 - (normalizedRot % 360)) / slice) % options.length; setTimeout(() => { resultDisplay.innerText = `Result: ${options[winningIndex]}`; }, 4000); }); renderWheel();