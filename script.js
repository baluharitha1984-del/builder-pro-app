const subjectInput = document.getElementById('subjectInput'); const listContainer = document.getElementById('listContainer'); const emptyMsg = document.getElementById('emptyMsg'); function addItem(type) { const val = subjectInput.value.trim(); if (!val) return; emptyMsg.classList.add('hidden'); const item = document.createElement('div'); item.className = `flex items-center justify-between p-4 rounded-xl ${type === 'good' ? 'bg-emerald-900/20 border border-emerald-500/30' : 'bg-rose-900/20 border border-rose-500/30'}`; item.innerHTML = `<span class="font-medium">${val}</span><div class="flex items-center gap-3"><span class="px-2 py-1 text-xs rounded-lg ${type === 'good' ? 'bg-emerald-500 text-black' : 'bg-rose-500 text-white'}">${type.toUpperCase()}</span><button class="delete-btn text-slate-500 hover:text-white transition-colors">✕</button></div>`; item.querySelector('.delete-btn').addEventListener('click', () => { item.remove(); if (listContainer.children.length === 1) emptyMsg.classList.remove('hidden'); }); listContainer.appendChild(item); subjectInput.value = ''; } document.getElementById('markGood').addEventListener('click', () => addItem('good')); document.getElementById('markBad').addEventListener('click', () => addItem('bad'));