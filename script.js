const words = ['అమ్మ', 'నాన్న', 'అన్నం', 'పుస్తకం', 'కలము', 'నది', 'సూర్యుడు', 'చంద్రుడు', 'గాలి', 'నీరు', 'ఆకాశం', 'భూమి', 'విద్య', 'ప్రేమ', 'శాంతి']; const display = document.getElementById('word-display'); const generateBtn = document.getElementById('generate-btn'); const resetBtn = document.getElementById('reset-btn'); const historyList = document.getElementById('history-list'); function updateDisplay(word) { display.textContent = word; display.classList.add('animate-pulse'); setTimeout(() => display.classList.remove('animate-pulse'), 500); } generateBtn.addEventListener('click', () => { const randomWord = words[Math.floor(Math.random() * words.length)]; updateDisplay(randomWord); const li = document.createElement('li'); li.className = 'text-slate-600 bg-slate-50 p-2 rounded-lg border border-slate-100'; li.textContent = randomWord; historyList.prepend(li); if (historyList.children.length > 5) historyList.removeChild(historyList.lastChild); }); resetBtn.addEventListener('click', () => { display.textContent = '---'; historyList.innerHTML = ''; });