const songs = [{ title: 'Butta Bomma', artist: 'Armaan Malik', id: 1 }, { title: 'Samajavaragamana', artist: 'Sid Sriram', id: 2 }, { title: 'Srivalli', artist: 'Sid Sriram', id: 3 }, { title: 'Oo Antava', artist: 'Indravathi Chauhan', id: 4 }]; let currentIdx = 0; const listEl = document.getElementById('song-list'); const titleEl = document.getElementById('now-playing-title'); const artistEl = document.getElementById('now-playing-artist'); const btnPlay = document.getElementById('play-pause-btn'); const renderList = () => { listEl.innerHTML = songs.map((s, idx) => `<li onclick="selectSong(${idx})" class="song-item p-4 rounded-xl border border-slate-800 flex justify-between items-center transition-all ${idx === currentIdx ? 'active-song' : ''}"><span>${s.title}</span><span class="text-slate-500">${s.artist}</span></li>`).join(''); }; const selectSong = (idx) => { currentIdx = idx; titleEl.innerText = songs[idx].title; artistEl.innerText = songs[idx].artist; renderList(); }; document.getElementById('play-pause-btn').addEventListener('click', () => { const icon = btnPlay.querySelector('i'); icon.classList.toggle('fa-play'); icon.classList.toggle('fa-pause'); }); document.getElementById('next-btn').addEventListener('click', () => selectSong((currentIdx + 1) % songs.length)); document.getElementById('prev-btn').addEventListener('click', () => selectSong((currentIdx - 1 + songs.length) % songs.length)); renderList();