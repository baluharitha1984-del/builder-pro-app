let currentAnswer = 0; let score = 0; const qBox = document.getElementById('question-box'); const ansInput = document.getElementById('answer-input'); const submitBtn = document.getElementById('submit-btn'); const nextBtn = document.getElementById('next-btn'); const scoreDisplay = document.getElementById('score-display'); const status = document.getElementById('status-indicator'); function generateQuestion() { const a = Math.floor(Math.random() * 12) + 1; const b = Math.floor(Math.random() * 12) + 1; currentAnswer = a * b; qBox.textContent = `${a} × ${b} = ?`; ansInput.value = ''; ansInput.focus(); } submitBtn.addEventListener('click', () => { if(parseInt(ansInput.value) === currentAnswer) { score++; status.textContent = 'Correct!'; status.className = 'text-emerald-500 font-bold'; } else { score = 0; status.textContent = 'Incorrect!'; status.className = 'text-rose-500 font-bold'; } scoreDisplay.textContent = score; submitBtn.classList.add('hidden'); nextBtn.classList.remove('hidden'); }); nextBtn.addEventListener('click', () => { generateQuestion(); nextBtn.classList.add('hidden'); submitBtn.classList.remove('hidden'); status.textContent = 'Ready'; status.className = 'text-emerald-500 font-medium'; }); generateQuestion();