let score = 0; let totalClicks = 0; let startTime = Date.now(); const scoreEl = document.getElementById('score'); const totalClicksEl = document.getElementById('total-clicks'); const timerEl = document.getElementById('timer'); const effEl = document.getElementById('efficiency'); const target = document.getElementById('click-target'); const feedback = document.getElementById('feedback-text'); const gameZone = document.getElementById('game-zone'); const resetBtn = document.getElementById('reset-btn'); gameZone.addEventListener('click', (e) => { score++; totalClicks++; scoreEl.innerText = score; totalClicksEl.innerText = totalClicks; feedback.style.left = e.offsetX + 'px'; feedback.style.top = e.offsetY + 'px'; feedback.classList.remove('animate-float'); void feedback.offsetWidth; feedback.classList.add('animate-float'); }); setInterval(() => { const elapsed = Math.floor((Date.now() - startTime) / 1000); timerEl.innerText = elapsed + 's'; if (elapsed > 0) effEl.innerText = (score / elapsed).toFixed(2) + ' c/s'; }, 1000); resetBtn.addEventListener('click', () => { score = 0; totalClicks = 0; startTime = Date.now(); scoreEl.innerText = '0'; totalClicksEl.innerText = '0'; effEl.innerText = '0 c/s'; });