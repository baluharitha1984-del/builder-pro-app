let currentNum = Math.floor(Math.random() * 100) + 1; let score = 0; const display = document.getElementById('current-number'); const scoreEl = document.getElementById('score'); const resultMsg = document.getElementById('result-msg'); display.textContent = currentNum; function updateGame(guess) { const nextNum = Math.floor(Math.random() * 100) + 1; let won = false; if (guess === 'high') won = nextNum >= currentNum; else won = nextNum <= currentNum; if (won) { score++; resultMsg.textContent = 'Correct! Next was ' + nextNum; resultMsg.className = 'text-center font-medium text-emerald-600'; } else { score = 0; resultMsg.textContent = 'Wrong! Next was ' + nextNum; resultMsg.className = 'text-center font-medium text-red-600'; } currentNum = nextNum; display.textContent = currentNum; scoreEl.textContent = score; } document.getElementById('btn-high').addEventListener('click', () => updateGame('high')); document.getElementById('btn-low').addEventListener('click', () => updateGame('low')); document.getElementById('btn-reset').addEventListener('click', () => { score = 0; scoreEl.textContent = '0'; resultMsg.textContent = ''; currentNum = Math.floor(Math.random() * 100) + 1; display.textContent = currentNum; });