document.getElementById('render-btn').addEventListener('click', () => { const n = parseInt(document.getElementById('num1').value) || 0; const d = parseInt(document.getElementById('den1').value) || 1; const box = document.getElementById('visual-box'); const percentage = (n / d) * 100; box.innerHTML = `<div class="w-full h-12 bg-slate-200 rounded-full overflow-hidden"><div class="fraction-bar bg-indigo-500 h-full" style="width: ${percentage}%"></div></div><p class="mt-4 text-sm">${n} out of ${d} pieces (${percentage.toFixed(1)}%)</p>`; }); document.getElementById('calc-btn').addEventListener('click', () => { const n1 = parseInt(document.getElementById('calc-n1').value) || 0; const d1 = parseInt(document.getElementById('calc-d1').value) || 1; const n2 = parseInt(document.getElementById('calc-n2').value) || 0; const d2 = parseInt(document.getElementById('calc-d2').value) || 1; const resN = (n1 * d2) + (n2 * d1); const resD = d1 * d2; const resBox = document.getElementById('result-box'); resBox.classList.remove('hidden'); resBox.innerText = `Result: ${resN}/${resD} ≈ ${(resN/resD).toFixed(3)}`; });