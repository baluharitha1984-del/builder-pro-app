let count = 0; const container = document.getElementById('log-container'); const trigger = document.getElementById('test-trigger'); const counter = document.getElementById('count-val'); trigger.addEventListener('click', () => { count++; counter.innerText = count; const log = document.createElement('div'); log.className = 'p-2 bg-white rounded border border-slate-200 text-slate-700 animate-in fade-in slide-in-from-left-2'; log.innerText = `Signal #${count} received at ${new Date().toLocaleTimeString()}`; container.prepend(log); });